spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/mantas_guajiras_db
    username: postgres
    password: 2318
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate

  open-in-view: false

  properties:
    hibernate:
      format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.flywaydb: INFO

jwt:
  secret: ${JWT_SECRET:mariaisabelbarrosrivera12345678923062006180920103005197809111979}
  expiration: ${JWT_EXPIRATION:86400000}

initial-admin:
  username: ${INITIAL_ADMIN_USERNAME:marisabarriv}
  phone: ${INITIAL_ADMIN_PHONE:3004521235}
  password: ${INITIAL_ADMIN_PASSWORD:mariaisabel1}